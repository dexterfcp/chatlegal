<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.legal.fcp.conderechoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_conderecho"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_conderecho"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VentanaInicialLogo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChatPrincipal" />
        <activity
            android:name=".SignificadoPal"
            android:label="@string/title_activity_significado_pal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PrinDiccionario"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
    </application>

</manifest>